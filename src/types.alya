# Types and data structures for jwt
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct JwtConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return JwtConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
